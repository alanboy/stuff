
name: devmatch
root: /opt/
root: /home/alan/code/DevMatch

windows:
  - frontend:
       layout: main-vertical
       root: ~/code/turing/frontend/developer-ui
       panes:
         - echo npm run start:local
         -
  - backend:
       layout: main-vertical
       root: ~/code/turing/backend
       panes:
         - echo yarn local
         -
  - claude-code:
       layout: main-vertical
       root: ~/code/turing/
       panes:
         - echo claude
         -
  - logs:
#
# windows:
#   - frontend:
#         layout: main-vertical
#         panes:
#             - cd frontend/developer-ui && yarn start
#             - cd frontend/developer-ui && echo logs
#   - backend:
#         layout: main-vertical
#         panes:
#             - cd backend && yarn local
#             - cd backend
#   - vscode: cd vscode-server && docker compose -f docker-compose.local.yml --env-file .env.local  up --build
#   - devmatcher: cd dev-matcher

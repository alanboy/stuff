name: enterpos
root: /opt/pos-erp

# Runs before everything. Use it to start daemons etc.
# pre: sudo /etc/rc.d/mysqld start

windows:
  - editor: vim /opt/pos-erp/
  - tests:
        layout: main-verticalp
        panes:
            - multitail --mergeall  /var/log/apache2/error.log /var/log/apache2/error.log /var/log/mx.caffeina.pos/access.log  /var/log/apache2/access.log /var/log/mysql/error.log
            - cd /opt/pos-erp/tests/phpunit
  - git: git status
